import styled from 'styled-components'

export const InfoPokemonStyle = styled.div`
  padding: 1rem;
  border-radius: .5rem;
  background-color: var(--black-secondary);
`

export const FlexStyle = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
`
