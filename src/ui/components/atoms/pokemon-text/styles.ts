import styled from 'styled-components'

export const PokemonTextStyle = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 2rem;
`

export const IdStyle = styled.h4`
  font-size: 1.8rem;
  font-weight: var(--font-weight-600);
  color: var(--violet-primary);
`

export const NameStyle = styled.h5`
  font-size: 1.6rem;
  font-weight: var(--font-weight-400);
  text-transform: capitalize;
`
